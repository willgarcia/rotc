name: azure-ts-aks
runtime: nodejs
description: Create an Azure Kubernetes Service (AKS) cluster
template:
  config:
    azure:environment:
      description: The Azure environment to use (`public`, `usgovernment`, `german`, `china`)
      default: public
      location: EastUS
